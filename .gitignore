# Dependencies
/node_modules
/.pnpm-store

# Production / Build outputs
/dist
/build

# Local environment & Temp files
.env
.env.local
.env.*.local
.vscode/
.idea/
.ds_store
Thumbs.db
masahati.pptx
BACKEND_PASSWORD_RESET_CONTRACT.md
dev.log
qa-server.log

# Vite / Bundler caches
/.vite
/.vite-temp

# Nested/embedded repo copy — must never be committed as gitlink
/masahati/